package Commands;

public class CommandFactory {

    public Command createCommand(String action) {
        switch (action) {
            //case "login":
            //return new LoginCommand();
            //break;
        }

        return null;
    }
}
